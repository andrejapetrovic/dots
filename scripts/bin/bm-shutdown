#!/bin/sh
options(){
	case $1 in
		"Shutdown") poweroff;;
		"Restart") reboot;;
		"Suspend") swaylock -f -c 000000; systemctl suspend;;
		"*") exit;;
	esac
}

echo "Shutdown\nRestart\nSuspend" | options $(bemenu -p "exit" -i)
